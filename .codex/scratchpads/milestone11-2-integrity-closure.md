# Milestone 11.2 Integrity Closure

## Metadata

- Starting commit: 05067b5b00e8e19585b0f21c1e2b26bc4ba5af81
- Branch: agent/milestone-11-1-external-paper-safety
- Started: 2026-07-17T15:59:18Z
- Last updated: 2026-07-17T15:59:18Z
- Working-tree status: clean except this scratchpad + docs/milestones/milestone11-2-full-integrity-closure.md (spec, untracked) at start
- Main test baseline: 1714 passed, 15 skipped (pytest tests/ -q), REPRODUCED FAILING under clean env before fix (see CI finding)
- Paper-runtime test baseline: 47 passed (paper_runtime/pytest tests/ -q)
- Type-check baseline: pyright run continue-on-error in CI; not yet captured locally
- CI baseline: main-tests FAILING on PR #7/#8 runs — root cause found: 2 tests depend on ambient ANTHROPIC_API_KEY env var (real key present in dev shell), non-hermetic. Fixed by stubbing cli_mod.load_config in both tests.

## Finding validation tracker

| ID | Finding | Current status | Severity | Evidence | Implementation | Tests |
|---|---|---|---|---|---|---|
| CI-1 | main-tests CI job failing on merged PR | CONFIRMED_AND_FIXED | high | gh run 29562160688 log: MISSING_CREDENTIALS vs PRICING_NOT_CONFIGURED | tests/unit/test_shadow_cli_provider_mode.py, tests/integration/test_milestone_7_1_shadow_integration.py: stub cli_mod.load_config so anthropic_api_key is present, isolating pricing-only failure from ambient env credentials | verified via `env -u ANTHROPIC_API_KEY ... pytest tests/ -q` -> 1714 passed |
| P3/P18 | `trg_paper_external_lookups_no_update` (v Milestone 11.1) only checked `OLD.consumed_by_retry_event_id IS NOT NULL` — allowed arbitrary field edits on an unconsumed row, and `CREATE TRIGGER IF NOT EXISTS` would never upgrade a pre-existing DB's stale trigger body | CONFIRMED_AND_FIXED | high | manual sqlite3 repro: legacy trigger permitted `UPDATE ... SET result='FOUND'` on unconsumed row | paper_books_schema.py: full field-immutability WHEN clause (IS-based, NULL-safe) + new `paper_books_trigger_versions` table + `_upgrade_triggers()` that DROPs+recreates on version bump, invoked in `apply_paper_books_schema` before the CREATE TRIGGER script | tests/unit/test_paper_external_lookup_trigger_migration.py (3 tests: legacy-fixture sanity, full contract post-upgrade, idempotent reopen) |

## Implementation checklist

(see spec doc checklist; tracked informally in tracker table above as work proceeds)

## Commands and reproductions

- `gh run list --limit 10` / `gh run view <id> --log-failed` to find CI failure root cause.
- `env -u ANTHROPIC_API_KEY -u ANTHROPIC_MODEL -u ANTHROPIC_BATCH_POLL_INTERVAL_SECONDS -u ALPACA_API_KEY -u ALPACA_API_SECRET -u ALPACA_IS_PAPER -u ALPACA_BASE_URL -u REDDIT_MCP_MODE -u REDDIT_MCP_COMMAND -u REDDIT_AUTH_MODE pytest tests/ -q --tb=short` reproduces clean-CI env; use this as the standard local re-run command going forward (dev shell has real ANTHROPIC_API_KEY + ALPACA_API_KEY/SECRET set).

## Files changed

| File | Purpose |
|---|---|
| tests/unit/test_shadow_cli_provider_mode.py | Stub load_config credentials to isolate pricing-check test from ambient env (CI-1) |
| tests/integration/test_milestone_7_1_shadow_integration.py | Same isolation fix (CI-1) |

| P4 | SQLite connections use Python's default (legacy) isolation_level implicitly | DESIGN_TRADEOFF_DOCUMENTED | high | grep confirms `connect()` never sets isolation_level; ~20+ repository modules use the `commit=False` participate-in-caller-transaction convention pervasively | Not doing the full `isolation_level=None` rewrite — blast radius spans every repository module in the codebase; instead hardened the concrete defect (see P5) | see P5 tests |
| P5 | Manual `BEGIN IMMEDIATE` call sites (lease.py x4, recurring_scheduler.py x3, external_broker.py x1) executed BEGIN IMMEDIATE outside/unguarded by their own try block; a pre-existing pending implicit transaction on the connection would make BEGIN IMMEDIATE itself raise, bypassing the except/rollback entirely | CONFIRMED_AND_FIXED | high | code inspection: `conn.execute("BEGIN IMMEDIATE")` sat before `try:` in all 7 call sites | new `database.begin_immediate()` helper (rolls back any pending implicit transaction first, then BEGIN IMMEDIATE); moved inside try in all 8 call sites (lease.py acquire/renew/release/force_release, recurring_scheduler.py acquire_recurring_lease/_claim_queue_items/_finalize_queue_items, external_broker.py fill-application) | tests/unit/test_transaction_discipline.py (6 tests: pending DML before BEGIN IMMEDIATE, rollback not silently committed, manual tx success, rollback leaves connection usable, failed op doesn't block a second real connection, busy_timeout configured) |

| P6 | Local simulated fill application (`execution.submit_and_simulate`) called save_fill/apply_buy_fill/settle_buy(or sell)/release_reservation/update_order_status each with default commit=True — 5 independent commits, not atomic; a crash between any two left fill_exists() falsely indicating completeness or a partially-applied fill | CONFIRMED_AND_FIXED | high | code inspection: all 5 repo/positions/cash_ledger calls in submit_and_simulate had no commit=False; update_order_status had no commit param at all | execution.py: wrapped fill-application (save_fill through update_order_status) in single begin_immediate/commit try-block, commit=False on every step; paper_books_repositories.py: added commit param to update_order_status | tests/unit/test_paper_books_local_fill_atomicity.py (6 tests: crash after save_fill/apply_buy_fill/settle_buy/release_reservation each leaves zero effects + successful retry, full-success invariant, replay-no-duplicate) |

| P7 | `cash_ledger.reserve_for_order`: check-then-act race — `available_cash()` read then `save_cash_ledger_entry` write with no lock between them; two concurrent BUY intents for the same book could both observe enough cash before either commits | CONFIRMED_AND_FIXED | high | code inspection; reproduced via real two-thread/two-connection test before fix (0/2 succeeded due to unrelated threading bug, then reproduced true race conceptually — see test) | cash_ledger.py: `reserve_for_order` now wraps the check+insert in `begin_immediate` book-scoped lock (self-contained when commit=True, the only mode in use) | tests/unit/test_paper_books_reservation_concurrency.py::test_two_buy_intents_cannot_collectively_overreserve, ::test_different_client_order_ids_still_serialize |
| P8 | `positions.reserve_shares_for_sell`: same check-then-act race for SELL share reservations at (book_id, symbol) scope, despite its docstring claiming "Atomically reserve" | CONFIRMED_AND_FIXED | high | code inspection | positions.py: same begin_immediate-wrapping pattern as P7 | tests/unit/test_paper_books_reservation_concurrency.py::test_two_sell_intents_cannot_collectively_overreserve (asserts quantity = available + reserved invariant post-race) |

| P9 | External preview/submit/retry (`_intent()` in external_broker.py) never checked the local `paper_book_orders.status` before proceeding — a locally FILLED/CANCELLED/REJECTED/EXPIRED intent could still be externally previewed/submitted | CONFIRMED_AND_FIXED | high | code inspection: `_intent()` loaded the order row and checked risk/order-type/TIF but never `intent["status"]` | external_broker.py: `_intent()` now rejects when `intent["status"] in TERMINAL_STATES` (reuses the existing FILLED/CANCELLED/REJECTED/EXPIRED constant so legitimate external in-flight retries — SUBMITTED/PARTIALLY_FILLED/etc, which get written into the same shared status column — remain eligible) | tests/unit/test_external_paper_broker.py::test_local_fill_blocks_subsequent_external_preview, ::test_local_cancel_blocks_subsequent_external_submit, ::test_pending_intent_still_previews_normally (regression sanity) |

| P10 | Order-scope lease: fixed 30s TTL, no heartbeat/renewal, no fencing generation, no owner+generation verification on writes; default runtime request timeout is also 30s (process_client.py) so a single runtime call could exhaust the entire lease with zero margin | CONFIRMED_AND_FIXED | high | code inspection: `_ORDER_LEASE_TTL_SECONDS = 30` fixed constant, `acquire_external_order_lease` returned bool with no generation tracking | schema: added `generation` column + versioned upgrade; repo: `acquire_external_order_lease` now returns generation, added `heartbeat_external_order_lease`/`verify_external_order_lease`, `release_external_order_lease` now generation-fenced; config: `external_broker.order_lease_ttl_seconds`/`order_lease_heartbeat_seconds` (validated TTL >= 2x heartbeat); `_order_lease` now yields an `OrderLeaseHandle` with `.heartbeat()`/`.verify()`; heartbeat calls added around the runtime calls in preview/submit | tests/unit/test_external_order_lease_fencing.py (5 tests: fresh acquire generation, second-acquire-blocked, heartbeat-survives-past-original-TTL, stale-owner-rejected-after-reclaim, verify-fails-after-expiry); tests/unit/test_external_paper_broker.py updated for new return type |

| P11 | External event-chain "current" queries used `ORDER BY created_at DESC, rowid DESC` instead of `scope_sequence DESC` — a backward clock jump between two writes could select the earlier event as current | CONFIRMED_AND_FIXED | medium | code inspection: `load_latest_external_order_event`, `load_latest_external_order_event_for_intent`, and the general list query all ordered by created_at/rowid, ignoring the already-populated `scope_sequence` column | paper_books_repositories.py: both "latest" queries now `ORDER BY scope_sequence DESC, created_at DESC, rowid DESC`; general listing now `ORDER BY scope_sequence, created_at, rowid` (SQLite sorts NULL lowest so legacy null-sequence rows never shadow sequenced ones) | tests/unit/test_external_event_sequence_ordering.py (3 tests: backward-clock, mixed-offset timestamps, legacy-null-sequence-never-shadows) |

| P12 | `_submit_once`'s post-submit fill sweep (`apply_external_fills` call right after a successful broker submission) was completely unprotected — a raised exception propagated straight out with zero persisted evidence | CONFIRMED_AND_FIXED | high | code inspection: no try/except around the call; confirmed via new test that a crash during fill application left no reconciliation record before the fix | external_broker.py `_submit_once`: wrapped the call in try/except persisting a critical `_persist_reconciliation` record (MALFORMED_BROKER_FILL or FILL_APPLICATION_FAILED) before re-raising | tests/unit/test_external_paper_broker.py::test_post_submit_fill_sweep_failure_persists_critical_reconciliation_before_raising |
| P13 | `cancel_external_paper_order`'s post-cancel fill sweep was likewise unprotected — a fill discovered in the cancel response that failed to apply would raise with no critical evidence, and would skip reservation-release/status-update (accidentally correct) but leave no visible blocker | CONFIRMED_AND_FIXED | high | code inspection | external_broker.py `cancel_external_paper_order`: same try/except pattern; reservation release and terminal status update remain downstream of the (now-protected) call so they still never execute on failure | tests/unit/test_external_paper_broker.py::test_post_cancel_fill_sweep_failure_persists_critical_and_withholds_reservation_release (asserts critical record persisted, order status not terminal, reservation not released) |

| P14 | Runtime-side `_validate_confirmed_long` (paper_runtime/dispatcher.py) checked only the raw confirmed position quantity, never subtracting shares already committed to other active open SELL orders for the same symbol — two concurrent SELL orders could together oversell | CONFIRMED_AND_FIXED | high | code inspection: no `list_open_orders()` call anywhere in the validation | dispatcher.py: `_validate_confirmed_long` now sums remaining (quantity - filled_quantity) across active-state open SELL orders for the same symbol (excluding the current client_order_id and terminal/BUY/other-symbol orders), using `_parse_exact_int` so a fractional broker-reported quantity fails closed; wired into all 3 call sites (`_op_submit_order`, `_op_PREVIEW_LIMIT_ORDER`, `_op_SUBMIT_LIMIT_ORDER`) | paper_runtime/tests/test_dispatcher_open_sell_accounting.py (9 tests: no-open-SELL allows full sell, 6-open blocks 5, 6-open allows 4, same-client-id-retry not double counted, BUY orders excluded, other-symbol orders excluded, terminal orders excluded, partial-fill only commits remainder, fractional quantity fails closed) |

| P15 | `_detect_duplicate_broker_order` explicitly skipped any candidate order whose client_order_id lacked the `epb-{book_id}-` prefix — a manually-created Alpaca order or one from another application was never detectable; also treated malformed/oversized recent-orders responses as "no duplicate" (fail-open) instead of fail-closed | CONFIRMED_AND_FIXED | high | code inspection: `if not candidate_client_id.startswith(...): continue` at the exact line the spec calls out | external_broker.py: removed the prefix-skip — now classifies same-prefix vs. non-prefixed duplicates separately (both flagged, with a distinct reason string); malformed/non-dict/oversized entries now raise `MALFORMED_RUNTIME_RESPONSE`, caught by `_reconcile_locked`'s existing outer wrapper and persisted as critical `RECONCILIATION_INTERNAL_ERROR` | tests/unit/test_external_paper_broker.py::test_manually_created_order_without_project_prefix_is_detected_as_duplicate, ::test_malformed_recent_orders_response_fails_closed |

| P16 | Recurring activation (`validate_activation_review`) read `provider_success_counts.real_provider_success_cycles` (counts a cycle if ANY real provider succeeded, even alongside another real provider's failure) instead of the already-computed, already-persisted `qualifying_real_provider_cycles` (requires ALL real-provider rows to succeed) | CONFIRMED_AND_FIXED | high | code inspection: `provider_provenance.py` line ~337 computes `success_count` from `SUCCEEDED in outcomes` (naive) separately from `qualifying_count` from `all(... == SUCCEEDED)` (strict); `soak_campaign.py` already persists both under `provider_success_counts`, but `recurring_scheduler.py` read the wrong key | recurring_scheduler.py `validate_activation_review`: now reads `qualifying_real_provider_cycles`; a review missing that key entirely (legacy, pre-field) fails closed requiring regeneration, rather than defaulting to 0 or falling back | tests/unit/test_recurring_paper_scheduler.py::test_activation_uses_qualifying_not_naive_success_count, ::test_activation_rejects_legacy_review_missing_qualifying_field; updated `ready_review` test fixture (was itself constructing a legacy-shaped review missing the field, which is why the fix initially broke 9 existing tests — fixed by adding a `provider_success_counts` override param) |

| P17 | No audited retry-preview-refresh action existed — a confirmed NOT_FOUND order became permanently stuck once its original preview's TTL lapsed before a retry could be issued | CONFIRMED_AND_FIXED (net-new feature) | medium | code inspection: no `refresh` function anywhere in external_broker.py | new `external_broker.refresh_retry_preview()`: read-only, no broker call, requires UNKNOWN state + fresh unconsumed authoritative NOT_FOUND matching current event's ambiguous_event_id/attempt_number/payload_hash/account_fingerprint + retry-limit not exceeded; creates a new preview row (new ID/expiry) without consuming the lookup; wired into CLI as `external-paper-refresh-retry-preview` (bypasses `_external_paper_cli`'s runtime spawn since it needs no runtime) | tests/unit/test_external_paper_broker.py::test_refresh_retry_preview_unblocks_retry_after_original_preview_expires (full UNKNOWN->NOT_FOUND->expire->refresh->retry->lookup-consumed-once scenario), ::test_refresh_retry_preview_rejected_once_broker_order_is_found, ::test_refresh_retry_preview_makes_no_broker_call |

| P19 | Runtime timeout response poisoning: on a request timeout, no mechanism prevented a late response from being consumed by a later, unrelated request | PARTIALLY_CONFIRMED_AND_FIXED | high | code inspection + new test proving the poisoning scenario; also discovered that a blanket "mark unhealthy on every timeout" fix breaks the codebase's own deliberate, tested get-order-after-timeout recovery pattern (`_RETRYABLE_ON_TIMEOUT`) | process_client.py: does NOT mark unhealthy on the timeout itself (preserves the legitimate retry-via-lookup pattern); instead marks unhealthy + tears down the transport the moment `parse_response_line` detects a request_id/operation mismatch (the actual proof of desync) — narrower than the spec's literal "restart after every timeout" but avoids regressing existing, intentional behavior; documented as a deliberate scope decision | tests/unit/test_runtime_client.py::test_timeout_alone_does_not_block_the_documented_recovery_lookup, ::test_late_stale_response_after_timeout_poisons_the_next_call_and_client_is_marked_unhealthy |
| P20 | `SubprocessTransport.terminate()` never joined the stdout/stderr pump threads or drained the stdout queue — repeated start/shutdown cycles could leak threads | CONFIRMED_AND_FIXED | medium | code inspection: no `.join()` calls anywhere on the pump threads | process_client.py: `terminate()` now always joins both pump threads (bounded timeout) and drains the stdout queue after the process exits, kill-then-wait added as a final fallback, streams explicitly closed | tests/unit/test_runtime_client.py::test_repeated_start_shutdown_cycles_join_pump_threads_without_leaking (uses a real `SubprocessTransport` + real trivial child process across 3 cycles) |

| P21 | `paper_runtime/configuration.py::_load_dotenv_if_present` used `load_dotenv(explicit_path, override=False)` directly — no allowlist enforcement (any key in the named file loaded into the environment), no absolute-path requirement, no permission check | CONFIRMED_AND_FIXED | high | code inspection + existing test literally named "loads only an explicitly named env file" whose own fixture mixed `ANTHROPIC_API_KEY` into the file and asserted credentials still loaded — proving unknown keys were silently accepted, not rejected | configuration.py: switched to `dotenv_values` (no env mutation) + explicit allowlist check (any unknown key rejects the *whole* file) + absolute-path requirement + symlink resolution + POSIX group/other-writable rejection; never raises (health/capabilities must stay answerable) — failed validation just means nothing from the file loads, leaving credentials absent | paper_runtime/tests/test_configuration.py: updated the pre-existing test to assert the new correct behavior + 3 new tests (whole-file-rejected-on-unknown-key, relative-path-rejected, group-writable-rejected) |

| P22 | `submit_credentialed_paper_order.py`'s recovery-lookup-after-ambiguous-submit used a bare `except Exception: recovered = None` — the recovery lookup's own failure reason was completely discarded (only the *original* submit failure got a persisted record) | CONFIRMED_AND_FIXED | medium | code inspection | submit_credentialed_paper_order.py: captures the recovery lookup's exception and persists it via `exec_repo.record_failure(..., stage="credentialed_recovery_lookup", ...)`, distinct from the original submit failure's own record | tests/unit/test_submit_credentialed_paper_order.py::test_submission_unknown_when_recovery_lookup_also_fails (extended to assert both failure stages are persisted) |

## Open issues

- Parts 2 (full prior-schema fixture matrix beyond the lookup trigger), 23-37 not yet started (Part 18 closed under P3). Given remaining time budget, moving to a faster inspect-classify pass for 23-31 (provider/config robustness) and 32-37 (settlement/legacy/schema-versioning/docs/offline-scenarios) — fixing only clear, narrow, high-confidence gaps; documenting the rest honestly as open in the final report rather than attempting shallow changes. Part 6's invariant-detection-on-historical-partial-fill-state (reconstruct-or-fail-closed for *pre-existing* corrupt rows from before this fix) not yet added — new writes are now atomic, but no scanner exists yet for legacy partial rows. Part 10's write-fencing was only wired into preview/submit heartbeat points, not verified via `lease.verify()` before every event-chain mutation across cancel/reconcile/retry — the acquire/release/heartbeat mechanism itself is fully fenced and tested, but per-write `verify()` gating inside those other 3 operations was not added.
- Given the scope of this milestone (37 parts / 44 acceptance criteria) continuing steadily part-by-part with the same rigor (inspect current main -> classify -> narrow fix -> test) rather than a shallow pass. Part 7's "reservation + SUBMISSION_REQUESTED committed in the same local transaction for external submission" not yet verified against external_broker.py's _submit_once (reserve_for_order now self-commits internally — need to check whether wrapping it together with the SUBMISSION_REQUESTED event write in one outer transaction is still required/possible with commit=False path).

## Resume instructions

- Last completed section: Part 3/18 (lookup trigger migration) and Part 4/5 (transaction discipline hardening). Both verified: full suite 1723 passed / paper_runtime 47 passed.
- Exact next task: Part 6 — locate and inspect the local simulated fill application code path (paper_books/execution.py) for atomicity vs. the external fill path pattern already used in external_broker.py (lines around the fill-application try/begin_immediate block).
- Tests not to repeat: tests/unit/test_paper_external_lookup_trigger_migration.py, tests/unit/test_transaction_discipline.py, tests/unit/test_shadow_cli_provider_mode.py, tests/integration/test_milestone_7_1_shadow_integration.py — all green.
- Remaining blockers: none yet. Note: pyright surfaces a large amount of pre-existing, unrelated noise in test_milestone_7_1_shadow_integration.py on nearly every edit anywhere in the repo (LSP background rescanning) — ignore unless the finding's file/line is one actually touched this session.

## Final status

- [ ] All findings classified — Parts 1,3-22,35(partial) done; Parts 2,23-34,36,37 not attempted
- [x] All confirmed blockers corrected (for the parts classified above)
- [x] Full tests pass — 1755 passed, 15 skipped (main); 59 passed (paper_runtime); verified under both ambient and clean-CI-simulated env
- [ ] Migration smoke passes — only the lookup-trigger fixture exists; full Part 2 fixture matrix not built
- [x] Concurrency tests pass — tests/unit/test_paper_books_reservation_concurrency.py (real 2-thread/2-connection races)
- [x] Documentation complete for parts done — docs/milestones/milestone11-2-integrity-closure.md written; README banners corrected
- [x] No live-trading path added

See docs/milestones/milestone11-2-integrity-closure.md for the full report and honest list of what remains open (Part 2 full fixture matrix, Parts 23-34, 36-37).
